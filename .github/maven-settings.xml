<?xml version="1.0" encoding="UTF-8"?>
<!--
  TEMPORARY: this settings file adds the Apache snapshots repository so CI can
  resolve org.apache.uima:parent-pom:19-SNAPSHOT (the unreleased parent this
  branch depends on). A project POM cannot declare the repository used to
  resolve its own parent, so it has to live in the Maven settings.

  Remove this file (and the settings flags in the workflows) once parent-pom
  19 is released to Maven Central and uimaj-parent is pinned to the release.
-->
<settings xmlns="http://maven.apache.org/SETTINGS/1.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.2.0 http://maven.apache.org/xsd/settings-1.2.0.xsd">
  <servers>
    <!-- Credentials for the Nexus deploy step (env vars set in the workflow). -->
    <server>
      <id>apache.snapshots.https</id>
      <username>${env.NEXUS_USERNAME}</username>
      <password>${env.NEXUS_PASSWORD}</password>
    </server>
  </servers>
  <profiles>
    <profile>
      <id>apache-snapshots</id>
      <repositories>
        <repository>
          <id>apache.snapshots</id>
          <name>Apache Snapshots</name>
          <url>https://repository.apache.org/content/repositories/snapshots</url>
          <releases>
            <enabled>false</enabled>
          </releases>
          <snapshots>
            <enabled>true</enabled>
          </snapshots>
        </repository>
      </repositories>
      <pluginRepositories>
        <pluginRepository>
          <id>apache.snapshots</id>
          <name>Apache Snapshots</name>
          <url>https://repository.apache.org/content/repositories/snapshots</url>
          <releases>
            <enabled>false</enabled>
          </releases>
          <snapshots>
            <enabled>true</enabled>
          </snapshots>
        </pluginRepository>
      </pluginRepositories>
    </profile>
  </profiles>
  <activeProfiles>
    <activeProfile>apache-snapshots</activeProfile>
  </activeProfiles>
</settings>
